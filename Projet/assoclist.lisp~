(defun list_assoc_make () nil)

(defun list_assoc_search (la key);;return the corresponding pair
  (if (null? la)
      nil
    (if (equal key (caar la))
	(car la)
      (list_assoc_search (cdr la) key))))

(defun list_assoc_add (la key value) 
  (if (null? la)
      (cons key (cons value nil)
  )
